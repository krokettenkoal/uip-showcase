// "propper" TS-file with types, triggers errors
let myName: string = "Luki";
myName = 3; // error

function sayHello(fromWho: string): number {
    console.log("Hello, from " + fromWho + " to " + myName);
    return 23; // because of the type, we can't forget to return a number
}

sayHello("BMT-3E");